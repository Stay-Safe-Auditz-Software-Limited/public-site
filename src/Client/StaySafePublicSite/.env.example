# This static site does not require environment variables.
# Add only values approved for public browser exposure, prefixed NUXT_PUBLIC_.
# Never put real credentials, API keys, connection strings, or private URLs here.
